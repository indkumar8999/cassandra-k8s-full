apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nosqlbench-histostats-pvc
  namespace: cassandra-lab
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  # Uses the cluster default StorageClass (e.g. k3s local-path). Set storageClassName if needed.

