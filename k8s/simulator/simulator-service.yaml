apiVersion: v1
kind: Service
metadata:
  name: cassandra-simulator
  namespace: cassandra-lab
spec:
  type: NodePort
  selector:
    app: cassandra-simulator
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
